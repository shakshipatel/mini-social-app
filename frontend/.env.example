# Frontend environment variables for Vite
# Copy this file to .env (or .env.local) and edit as needed

# Base API URL the frontend should use (include /api at the end)
VITE_API_URL=http://localhost:4000/api
